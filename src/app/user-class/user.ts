export class User {
    constructor(public login:string, public avatar_url :string, public following :number, public followers:number, public location :string, public repos:number, public public_repos :number ){} 
}
